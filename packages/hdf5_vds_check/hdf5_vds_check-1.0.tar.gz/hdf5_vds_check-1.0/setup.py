#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

install_requires = \
['h5py >=2.9.0']

entry_points = \
{'console_scripts': ['hdf5-vds-check = hdf5_vds_check:main']}

setup(name='hdf5_vds_check',
      version='1.0',
      description='Check access to the source files of virtual datasets',
      author='European XFEL GmbH',
      author_email='cas-support@xfel.eu',
      url='https://github.com/European-XFEL/hdf5-vds-check',
      py_modules=['hdf5_vds_check'],
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
