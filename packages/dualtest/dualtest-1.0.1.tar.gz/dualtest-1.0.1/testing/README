Do not install this package.

This directory contains the tests for dualtest and serves as an example of it's
use. Since a setup.py file is needed to build C extensions for testing, this
sub-package is used to build and test them.

To run the tests run:
    python setup.py -q test
