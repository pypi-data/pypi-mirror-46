#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['pytest_markdown']

package_data = \
{'': ['*']}

install_requires = \
['pytest>=3.6.0', 'CommonMark~=0.8.1']

entry_points = \
{'pytest11': ['markdown = pytest_markdown.plugin']}

setup(name='pytest_markdown',
      version='0.0.7',
      description='A py.test plugin that runs tests its found in your Markdown files.',
      author='John Carr',
      author_email='john.carr@unrouted.co.uk',
      url='https://github.com/Jc2k/pytest-markdown',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
