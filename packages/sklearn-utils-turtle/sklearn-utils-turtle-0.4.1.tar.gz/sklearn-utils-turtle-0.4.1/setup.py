from setuptools import setup
import setuptools
setup(
    name='sklearn-utils-turtle',    # This is the name of your PyPI-package.
    version='0.4.1',                          # Update the version number for new releases
    packages=setuptools.find_packages(),
)