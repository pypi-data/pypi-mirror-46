#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['cherry_picker']

package_data = \
{'': ['*']}

install_requires = \
['click>=6.0', 'gidgethub', 'requests', 'toml']

extras_require = \
{'dev': ['pytest']}

entry_points = \
{'console_scripts': ['cherry_picker = '
                     'cherry_picker.cherry_picker:cherry_pick_cli']}

setup(name='cherry_picker',
      version='1.3.2',
      description='Backport CPython changes from master to maintenance branches.',
      author='Mariatta Wijaya',
      author_email='mariatta.wijaya@gmail.com',
      url='https://github.com/python/core-workflow/tree/master/cherry_picker',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
