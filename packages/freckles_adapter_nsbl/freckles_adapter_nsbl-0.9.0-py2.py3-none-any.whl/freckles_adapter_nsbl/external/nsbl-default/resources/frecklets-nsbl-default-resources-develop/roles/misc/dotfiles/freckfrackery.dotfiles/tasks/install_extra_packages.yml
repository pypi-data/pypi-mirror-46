---


- name: "installing extra packages for: {{ dotfiles_folder }}"
  include_role:
    name: freckfrackery.install-pkgs
  vars:
    install_packages: "{{ dotfiles_packages }}"
    install_ignore_errors: "{{ dotfiles_ignore_install_errors }}"
