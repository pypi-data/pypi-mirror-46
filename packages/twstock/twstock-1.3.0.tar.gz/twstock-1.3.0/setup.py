#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['twstock', 'twstock.cli', 'twstock.codes', 'twstock.mock']

package_data = \
{'': ['*']}

install_requires = \
['requests']

entry_points = \
{'console_scripts': ['twstock = twstock.cli:run']}

setup(name='twstock',
      version='1.3.0',
      description='Taiwan Stock Opendata with realtime - twstock',
      author='Louie Lu',
      author_email='git@louie.lu',
      url='https://github.com/mlouielu/twstock',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3',
     )
