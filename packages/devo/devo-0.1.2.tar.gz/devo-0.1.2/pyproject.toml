[tool.poetry]
name = "devo"
version = "0.1.2"
description = ""
authors = ["Dominic Werner <aka.vince@gmail.com>"]
license = "MIT"

[tool.poetry.scripts]
devo = 'devo.main:cli'

[tool.poetry.dependencies]
python = "^3.6"
click = "^7.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
