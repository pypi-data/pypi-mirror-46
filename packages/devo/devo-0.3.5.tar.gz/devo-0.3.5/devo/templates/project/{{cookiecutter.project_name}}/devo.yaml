project_name: {{ cookiecutter.project_name }}
ejected: false
registry:
  url: {{ cookiecutter.registry_url }}
  namespace: {{ cookiecutter.registry_namespace }}
  {% if cookiecutter.registry_url %}
  image: {{ cookiecutter.registry_url }}/{{ cookiecutter.registry_namespace }}/{{ cookiecutter.project_name }}
  {% else %}
  image: {{ cookiecutter.registry_namespace }}/{{ cookiecutter.project_name }}
  {% endif %}
database:
  enabled: false
gitlab:
  enabled: false
  url: ""
  group: ""
  project: ""
  project_id: ""
  namespace_id: ""
