apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ project_name }}-app
spec:
  template:
    spec:
      containers:
        - name: {{ project_name }}-app
          image: {{ registry_image }}/test
