- name: Creating Worker auxiliary directories...
  file:
    path: '{{ item }}'
    state: directory
    recurse: yes
  with_items:
    - '{{ settings.worker.stdout_log_filename | dirname }}'
    - '{{ settings.worker.stderr_log_filename | dirname }}'
  ignore_errors: yes
  failed_when: False
