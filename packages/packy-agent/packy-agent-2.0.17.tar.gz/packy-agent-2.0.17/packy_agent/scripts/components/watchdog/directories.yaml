- name: Creating Watchdog auxiliary directories...
  file:
    path: '{{ item }}'
    state: directory
    recurse: yes
  with_items:
    - '{{ settings.watchdog.stdout_log_filename | dirname }}'
    - '{{ settings.watchdog.stderr_log_filename | dirname }}'
  ignore_errors: yes
  failed_when: False
