- name: Creating uWSGI configuration file...
  vars:
    uwsgi_pid_file: '{{ settings.console.uwsgi.pid_filename }}'
  template:
    src: ../../../configuration/templates/uwsgi.ini.j2
    dest: '{{ settings.console.uwsgi.configuration_filename }}'
