- name: Running command with virtualenvwrapper active...
  shell: bash -c 'export VIRTUALENVWRAPPER_PYTHON={{ virtualenvwrapper_python }} &&
                  export WORKON_HOME={{ venvs_dir }} &&
                  source /usr/local/bin/virtualenvwrapper.sh &&
                  {{ command }}'
