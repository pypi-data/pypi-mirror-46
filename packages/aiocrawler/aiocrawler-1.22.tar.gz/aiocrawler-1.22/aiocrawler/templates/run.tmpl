# coding: utf-8
# Date      : $date
# PROJECT   : $project_name
# File      : $filename
from aiocrawler.engine import Engine
from settings import $settings_name


def run(spider_module):
    settings = $settings_name()
    spider = spider_module(settings)
    engine = Engine(
        spider=spider,
        settings=settings
    )
    engine.run()
