rules:
  - tablename: device
    keys: serial
    support:
      - insert
      - delete
      - update
      - select

  - tablename: role
    keys: role_id
    support:
      - insert
      - delete
      - update
      - select