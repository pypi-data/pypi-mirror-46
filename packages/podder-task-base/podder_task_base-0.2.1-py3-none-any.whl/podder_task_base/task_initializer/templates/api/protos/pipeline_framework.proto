syntax = "proto3";

package pipeline_framework;

service {{ task_class }}Api {
    rpc execute (TaskResponse) returns (TaskResponse) {}
}

message TaskResponse {
    string dag_id = 1;
    repeated Result results = 2;
}

message Result {
    string job_id = 1;
    string job_data = 2;
}
